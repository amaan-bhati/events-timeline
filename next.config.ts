import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['assets.aceternity.com', 'drive.google.com', 'res.cloudinary.com', 'asset.cloudinary.com'],
  },
};

export default nextConfig;
